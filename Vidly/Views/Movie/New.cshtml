@model Vidly.ViewModels.NewMovieViewModel

@{
    ViewBag.Title = "New";
}

<h2>New Movie</h2>

@using (Html.BeginForm("Save", "Movie"))
{
    @Html.ValidationSummary(true, "Please fix the following errors. ")

    <div class="form-group">
        @Html.LabelFor(c => c.Name)
        @Html.TextBoxFor(c => c.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Name)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.ReleaseDate)
        @Html.TextBoxFor(c => c.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.ReleaseDate)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.GenreId)
        @Html.DropDownListFor(c => c.GenreId, new SelectList(Model.Genres, "Id", "Type"), "Select Genre", new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.GenreId)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.Stock)
        @Html.TextBoxFor(c => c.Stock, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Stock)
    </div>

    @Html.HiddenFor(c => c.Id)
    @Html.AntiForgeryToken()
    <button class="btn btn-primary">Save</button>
}


@section scripts
{
    @Scripts.Render("~/bundles/jqueryval");
}
